# SQL اختیاری

| فایل | توضیح |
| --- | --- |
| `cron-sync-external-scores.sql` | **راهنما** — تنظیم Cron از Dashboard (SQL اجرایی نیست) |

Cron با pg_cron (قالب SQL): **`sql/archive/cron-job-sync-external-scores.sql`** — فقط اگر job را از نو می‌خواهید بسازید.

مهاجرت portal قدیمی: **`sql/archive/unify-portal-users.sql`** — فقط اگر جداول `admin_portal_*` دارید.
